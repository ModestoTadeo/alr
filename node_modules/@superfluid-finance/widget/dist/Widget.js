"use client";
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { Alert, AlertTitle, Container } from "@mui/material";
import { extendedSuperTokenList } from "@superfluid-finance/tokenlist";
import { useEffect, useState } from "react";
import { ErrorBoundary } from "react-error-boundary";
import { defaultNetworkAssets } from "./core/index.js";
import { WidgetCore } from "./WidgetCore.js";
/**
 * The entrypoint to the Superfluid widget.
 */
export function Widget({ productDetails = defaultProductDetails, tokenList = extendedSuperTokenList, stepper = defaultStepper, existentialNFT = {
    name: "",
    symbol: "",
    owner: "",
    deployments: {},
}, type = "page", networkAssets = defaultNetworkAssets, personalData = defaultPersonalData, ...rest }) {
    const props = {
        productDetails,
        existentialNFT,
        tokenList,
        stepper,
        type,
        networkAssets,
        personalData,
        ...rest,
    };
    const [mounted, setMounted] = useState(false);
    useEffect(() => setMounted(true), []);
    return (_jsx(ErrorBoundary, { fallback: _jsx(Container, { maxWidth: "sm", children: _jsxs(Alert, { severity: "error", variant: "standard", children: [_jsx(AlertTitle, { children: "Oops! The Checkout Widget crashed" }), _jsx("p", { children: "Apologies for any inconvenience caused, but the Checkout Widget just experienced an unexpected problem and failed to load." }), _jsx("p", { children: "We appreciate your understanding and patience. You might want to try reloading the page or come back later. If the issue persists, please don't hesitate to get in touch with us." })] }) }), children: mounted && _jsx(WidgetCore, { ...props }) }));
}
const defaultProductDetails = {
    name: "",
};
const defaultStepper = { orientation: "vertical" };
const defaultPersonalData = [];
